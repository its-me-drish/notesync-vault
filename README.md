# NoteSync Vault

Markdown note vault with tags, full-text search and offline-first sync.

## Stack
- Node.js + Express (API)
- MongoDB + Mongoose
- React + Vite (client)
- JWT auth, Jest + Supertest
